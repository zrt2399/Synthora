<ResourceDictionary xmlns="https://github.com/avaloniaui"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:panels="clr-namespace:Synthora.Controls.DragTabControl.Panels"
                    x:ClassModifier="internal"
                    x:CompileBindings="True">

    <ControlTheme x:Key="AddItemCommandButtonTheme" TargetType="Button">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="CornerRadius" Value="2.5" />
        <Setter Property="Margin" Value="2,0,4,2" />
        <!-- <Setter Property="Height" Value="28" />
        <Setter Property="Width" Value="28" />-->
        <Setter Property="Template">
            <ControlTemplate>
                <Border x:Name="border"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        CornerRadius="{TemplateBinding CornerRadius}">

                    <Viewbox Width="24"
                             Height="24"
                             Stretch="Uniform">
                        <Canvas Width="24" Height="24">
                            <Path Data="M19,12.5 H12.5 V19 H11.5 V12.5 H5 V11.5 H11.5 V5 H12.5 V11.5 H19 Z" Fill="{TemplateBinding Foreground}" />
                        </Canvas>
                    </Viewbox>
                </Border>
            </ControlTemplate>
        </Setter>
        <Style Selector="^:pointerover">
            <Setter Property="Foreground" Value="{DynamicResource AddItemCommandButtonForegroundPointerOverBrush}" />
            <Setter Property="Background" Value="{DynamicResource AddItemCommandButtonBackgroundPointerOverBrush}" />
        </Style>
 
        <Style Selector="^:pressed">
            <Setter Property="Background" Value="{DynamicResource AddItemCommandButtonBackgroundPressedBrush}" />
        </Style>
    </ControlTheme>

    <ControlTheme x:Key="WindowThumbStyle" TargetType="Thumb">
        <Setter Property="HorizontalAlignment" Value="Stretch" />
        <Setter Property="VerticalAlignment" Value="Stretch" />
        <Setter Property="IsHitTestVisible" Value="True" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate>
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="{TemplateBinding CornerRadius}" />
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </ControlTheme>

    <ControlTheme x:Key="{x:Type DragTabControl}" TargetType="DragTabControl">
        <Setter Property="Background" Value="{DynamicResource DragTabControlBackgroundBrush}" />

        <Setter Property="AdjacentHeaderItemOffset" Value="-8" />
        <Setter Property="TabItemWidth" Value="250" />

        <Setter Property="Template">
            <ControlTemplate>
                <Border HorizontalAlignment="{TemplateBinding HorizontalAlignment}"
                        VerticalAlignment="{TemplateBinding VerticalAlignment}"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        CornerRadius="{TemplateBinding CornerRadius}">

                    <DockPanel>
                        <panels:TopPanel Name="PART_TopPanel"
                                         Background="{TemplateBinding Background}"
                                         DockPanel.Dock="Top">
                            <ContentPresenter Name="PART_LeftContent" Content="{TemplateBinding LeftContent}" />

                            <Thumb Name="PART_LeftDragWindowThumb"
                                   MinWidth="{TemplateBinding LeftThumbWidth}"
                                   Theme="{StaticResource WindowThumbStyle}" />

                            <ItemsPresenter Name="PART_ItemsPresenter" ItemsPanel="{TemplateBinding ItemsPanel}" />

                            <Button Name="PART_AddItemButton"
                                    Command="{Binding AddItemCommand, RelativeSource={RelativeSource TemplatedParent}}"
                                    IsVisible="{TemplateBinding ShowAddButton}"
                                    Theme="{StaticResource AddItemCommandButtonTheme}" />

                            <Thumb Name="PART_RightDragWindowThumb"
                                   MinWidth="{TemplateBinding RightThumbWidth}"
                                   Theme="{StaticResource WindowThumbStyle}" />

                            <ContentPresenter Name="PART_RightContent" Content="{TemplateBinding RightContent}" />
                        </panels:TopPanel>

                        <Panel Background="{DynamicResource DragTabItemSelectedBackgroundBrush}">
                            <ContentPresenter Name="PART_SelectedContentHost2"
                                              HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}"
                                              IsVisible="False" />
                            <ContentPresenter Name="PART_SelectedContentHost"
                                              HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}" />
                        </Panel>
                    </DockPanel>
                </Border>
            </ControlTemplate>
        </Setter>
    </ControlTheme>
</ResourceDictionary> 