<ResourceDictionary xmlns="https://github.com/avaloniaui"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    x:ClassModifier="internal"
                    x:CompileBindings="True">

    <x:Double x:Key="TreeMenuBarHeight">36</x:Double>
    <x:Double x:Key="TreeMenuOpenPaneThemeLength">320</x:Double>
    <SolidColorBrush x:Key="TreeMenuHeaderBarBackground" Color="{DynamicResource ThemeBackgroundColor}" />

    <StreamGeometry x:Key="TreeMenuPaneButtonIcon">
        M432 176H80c-8.8 0-16-7.2-16-16s7.2-16 16-16h352c8.8 0 16 7.2 16 16s-7.2 16-16 16zM432 272H80c-8.8 0-16-7.2-16-16s7.2-16 16-16h352c8.8 0 16 7.2 16 16s-7.2 16-16 16zM432 368H80c-8.8 0-16-7.2-16-16s7.2-16 16-16h352c8.8 0 16 7.2 16 16s-7.2 16-16 16z
    </StreamGeometry>

    <ControlTheme x:Key="{x:Type TreeMenu}" TargetType="TreeMenu">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="MenuMargin" Value="28,0" />
        <Setter Property="PaneBackground" Value="{DynamicResource ThemeControlLowBrush}" />
        <Setter Property="OpenPaneLength" Value="{DynamicResource TreeMenuOpenPaneThemeLength}" />
        <Setter Property="CompactPaneLength" Value="{DynamicResource TreeMenuBarHeight}" />
        <Setter Property="HeaderHeight" Value="{DynamicResource TreeMenuBarHeight}" />
        <Setter Property="HeaderBackground" Value="{DynamicResource TreeMenuHeaderBarBackground}" />

        <Setter Property="DisplayMode" Value="{OnFormFactor Default=Inline, Mobile=Overlay, x:TypeArguments=SplitViewDisplayMode}" />
        <Setter Property="UseLightDismissOverlayMode" Value="{OnFormFactor Default=False, Mobile=True, x:TypeArguments=x:Boolean}" />
        <Setter Property="IsPaneOpen" Value="{OnFormFactor Default=True, Mobile=False, x:TypeArguments=x:Boolean}" />
        <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Auto" />
        <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto" />
        <Setter Property="ScrollViewer.IsScrollChainingEnabled" Value="True" />
        <Setter Property="Template">
            <ControlTemplate>
                <SplitView Background="{TemplateBinding Background}"
                           BorderBrush="{TemplateBinding BorderBrush}"
                           BorderThickness="{TemplateBinding BorderThickness}"
                           CompactPaneLength="{TemplateBinding CompactPaneLength}"
                           DisplayMode="{TemplateBinding DisplayMode}"
                           IsPaneOpen="{TemplateBinding IsPaneOpen,
                                                        Mode=TwoWay}"
                           OpenPaneLength="{TemplateBinding OpenPaneLength}"
                           PaneBackground="{TemplateBinding PaneBackground}"
                           UseLightDismissOverlayMode="{TemplateBinding UseLightDismissOverlayMode}">
                    <SplitView.Pane>
                        <Grid RowDefinitions="Auto,*,Auto">
                            <ContentPresenter Content="{TemplateBinding PaneHeader}" ContentTemplate="{TemplateBinding PaneHeaderTemplate}" />
                            <ScrollViewer Grid.Row="1"
                                          Margin="{TemplateBinding MenuMargin}"
                                          AllowAutoHide="{TemplateBinding ScrollViewer.AllowAutoHide}"
                                          BringIntoViewOnFocusChange="{TemplateBinding ScrollViewer.BringIntoViewOnFocusChange}"
                                          HorizontalScrollBarVisibility="{TemplateBinding ScrollViewer.HorizontalScrollBarVisibility}"
                                          IsDeferredScrollingEnabled="{TemplateBinding ScrollViewer.IsDeferredScrollingEnabled}"
                                          IsScrollChainingEnabled="{TemplateBinding ScrollViewer.IsScrollChainingEnabled}"
                                          KeyboardNavigation.TabNavigation="Once"
                                          VerticalScrollBarVisibility="{TemplateBinding ScrollViewer.VerticalScrollBarVisibility}">
                                <ItemsPresenter Name="PART_ItemsPresenter" ItemsPanel="{TemplateBinding ItemsPanel}" />
                            </ScrollViewer>
                            <ContentPresenter Grid.Row="2"
                                              Content="{TemplateBinding PaneFooter}"
                                              ContentTemplate="{TemplateBinding PaneFooterTemplate}" />
                        </Grid>
                    </SplitView.Pane>
                    <DockPanel>
                        <Border Height="{TemplateBinding HeaderHeight}"
                                Background="{TemplateBinding HeaderBackground}"
                                DockPanel.Dock="Top">
                            <DockPanel HorizontalSpacing="8">
                                <ToggleButton Name="PART_PaneButton"
                                              Width="{Binding $self.Bounds.Height}"
                                              AutomationProperties.Name="Toggle navigation pane"
                                              BorderBrush="{x:Null}"
                                              BorderThickness="0"
                                              ButtonAttach.ShowShadowOnInteraction="False"
                                              Classes="Icon"
                                              CornerRadius="0"
                                              IsChecked="{TemplateBinding IsPaneOpen,
                                                                          Mode=TwoWay}"
                                              ToolTip.Tip="{DynamicResource StringTreeMenuToggleNavigationPane}">
                                    <Panel>
                                        <PathIcon HorizontalAlignment="Center"
                                                  VerticalAlignment="Center"
                                                  Data="{DynamicResource TreeMenuPaneButtonIcon}"
                                                  IsVisible="{Binding PaneIcon, RelativeSource={RelativeSource TemplatedParent}, Converter={x:Static ObjectConverters.IsNull}}" />
                                        <ContentPresenter Name="PART_PaneIconPresenter"
                                                          HorizontalAlignment="Center"
                                                          VerticalAlignment="Center"
                                                          Content="{TemplateBinding PaneIcon}"
                                                          ContentTemplate="{TemplateBinding PaneIconTemplate}"
                                                          IsVisible="{Binding PaneIcon, RelativeSource={RelativeSource TemplatedParent}, Converter={x:Static ObjectConverters.IsNotNull}}" />
                                    </Panel>
                                </ToggleButton>
                                <ContentPresenter Name="PART_TitlePresenter"
                                                  VerticalAlignment="Center"
                                                  Content="{TemplateBinding Header}"
                                                  ContentTemplate="{TemplateBinding HeaderTemplate}"
                                                  FontSize="{DynamicResource FontSizeLarge}"
                                                  IsVisible="{TemplateBinding Header,
                                                                              Converter={x:Static ObjectConverters.IsNotNull}}" />
                            </DockPanel>
                        </Border>
                        <ContentPresenter Name="PART_SelectedContentPresenter" Margin="{TemplateBinding Padding}" />
                    </DockPanel>
                </SplitView>
            </ControlTemplate>
        </Setter>
        <Style Selector="^.Border">
            <Setter Property="BorderBrush" Value="{DynamicResource SynthoraBorderBrush}" />
            <Setter Property="BorderThickness" Value="{DynamicResource ThemeBorderThickness}" />
        </Style>
        <Style Selector="^.Shadow">
            <Setter Property="ClipToBounds" Value="False" />
            <Setter Property="BoxShadowAttach.BoxShadow" Value="{DynamicResource DefaultBoxShadow}" />
        </Style>
    </ControlTheme>

</ResourceDictionary>