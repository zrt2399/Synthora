<ResourceDictionary xmlns="https://github.com/avaloniaui"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    x:ClassModifier="internal"
                    x:CompileBindings="True">

    <Design.PreviewWith>
        <Border Padding="20">
            <StackPanel Spacing="20">
                <Banner Content="A banner without icon type." />
                <Banner Content="Pull operation failed. Check the Output window for details." IconType="Error" />
                <Banner Content="Do you want to continue with this operation?" IconType="Question" />
                <Banner Content="Operation completed successfully!" IconType="Success" />
                <Banner Content="Note: This operation may take a long time." IconType="Warning" />
                <Banner Content="This banner shows an accent bar."
                        IconType="Warning"
                        ShowAccentBar="True" />
                <Banner Content="This banner hides the icon."
                        IconType="Information"
                        ShowIcon="False" />
                <Banner Content="System information has been updated." IconType="Information" />
            </StackPanel>
        </Border>
    </Design.PreviewWith>

    <ControlTheme x:Key="{x:Type Banner}" TargetType="Banner">
        <Setter Property="Padding" Value="{DynamicResource ThemeControlPadding}" />
        <Setter Property="CornerRadius" Value="{DynamicResource ThemeBorderCornerRadiusNormal}" />
        <Setter Property="HorizontalContentAlignment" Value="Left" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="Template">
            <ControlTemplate>
                <Border Name="RootBorder"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        BoxShadow="{TemplateBinding (BoxShadowAttach.BoxShadow)}"
                        CornerRadius="{TemplateBinding CornerRadius}">
                    <DockPanel>
                        <!--  Accent Bar  -->
                        <Border Width="4"
                                Margin="8,8,0,8"
                                VerticalAlignment="Stretch"
                                Background="{TemplateBinding BorderBrush}"
                                CornerRadius="{TemplateBinding CornerRadius}"
                                DockPanel.Dock="Left"
                                IsVisible="{TemplateBinding ShowAccentBar}" />

                        <!--  Icon  -->
                        <TipIcon Name="Icon"
                                 Width="20"
                                 Height="20"
                                 Margin="8,8,0,8"
                                 VerticalAlignment="Top"
                                 DockPanel.Dock="Left"
                                 IconType="{TemplateBinding IconType}" />

                        <!--  Close Button  -->
                        <Button Width="20"
                                Height="20"
                                Margin="0,8,8,8"
                                VerticalAlignment="Top"
                                Command="{Binding $parent[Banner].Close}"
                                DockPanel.Dock="Right"
                                IsVisible="{TemplateBinding ShowCloseButton}"
                                Theme="{StaticResource IconButtonTheme}"
                                ToolTip.Tip="{DynamicResource StringCloseText}">
                            <PathIcon Width="12"
                                      Height="12"
                                      Data="{DynamicResource TextBoxClearButtonData}" />
                        </Button>

                        <!--  Content  -->
                        <ContentPresenter Name="PART_ContentPresenter"
                                          Margin="{TemplateBinding Padding}"
                                          HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                          Content="{TemplateBinding Content}"
                                          RecognizesAccessKey="True"
                                          TextWrapping="{TemplateBinding TextWrapping}" />
                    </DockPanel>
                </Border>
            </ControlTemplate>
        </Setter>

        <Style Selector="^:none">
            <Setter Property="BorderBrush" Value="{DynamicResource SynthoraBorderBrush}" />
            <Setter Property="Background" Value="{DynamicResource SynthoraLowBrush}" />
            <Style Selector="^ /template/ TipIcon#icon">
                <Setter Property="IsVisible" Value="False" />
            </Style>
        </Style>

        <Style Selector="^:information">
            <Setter Property="BorderBrush" Value="{DynamicResource PrimaryBrush}" />
            <Setter Property="Background" Value="{DynamicResource InformationTipBackgroundBrush}" />
        </Style>

        <Style Selector="^:question">
            <Setter Property="BorderBrush" Value="{DynamicResource QuestionBrush}" />
            <Setter Property="Background" Value="{DynamicResource QuestionTipBackgroundBrush}" />
        </Style>

        <Style Selector="^:success">
            <Setter Property="BorderBrush" Value="{DynamicResource SuccessBrush}" />
            <Setter Property="Background" Value="{DynamicResource SuccessTipBackgroundBrush}" />
        </Style>

        <Style Selector="^:warning">
            <Setter Property="BorderBrush" Value="{DynamicResource WarningBrush}" />
            <Setter Property="Background" Value="{DynamicResource WarningTipBackgroundBrush}" />
        </Style>

        <Style Selector="^:error">
            <Setter Property="BorderBrush" Value="{DynamicResource ErrorBrush}" />
            <Setter Property="Background" Value="{DynamicResource ErrorTipBackgroundBrush}" />
        </Style>

        <Style Selector="^[ShowIcon=False] /template/ TipIcon#icon">
            <Setter Property="IsVisible" Value="False" />
        </Style>

        <Style Selector="^[IsClosed=True]">
            <Setter Property="IsVisible" Value="False" />
        </Style>

        <Style Selector="^.Border">
            <Setter Property="BorderThickness" Value="{DynamicResource ThemeBorderThickness}" />
        </Style>

        <Style Selector="^:disabled">
            <Setter Property="Opacity" Value="{DynamicResource ThemeDisabledLowOpacity}" />
        </Style>
    </ControlTheme>

</ResourceDictionary>