<ResourceDictionary xmlns="https://github.com/avaloniaui"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:controls="using:Synthora.Controls"
                    x:ClassModifier="internal"
                    x:CompileBindings="True">

    <ControlTheme x:Key="SeparatorBaseTheme" TargetType="Separator">
        <Setter Property="Focusable" Value="False" />
        <Setter Property="Template">
            <ControlTemplate>
                <Panel Background="{TemplateBinding Background}">
                    <controls:PixelAlignedRectangle Width="{TemplateBinding Width}"
                                                    Height="{TemplateBinding Height}"
                                                    Margin="{TemplateBinding Padding}"
                                                    Fill="{TemplateBinding Foreground}" />
                </Panel>
            </ControlTemplate>
        </Setter>
    </ControlTheme>

    <ControlTheme x:Key="{x:Type Separator}"
                  BasedOn="{StaticResource SeparatorBaseTheme}"
                  TargetType="Separator">
        <Setter Property="Foreground" Value="{DynamicResource ThemeBorderLowBrush}" />
        <Setter Property="Margin" Value="{DynamicResource MenuItemHorizontalSeparatorMargin}" />
        <Setter Property="Height" Value="1" />
    </ControlTheme>

    <ControlTheme x:Key="HorizontalSeparatorTheme"
                  BasedOn="{StaticResource SeparatorBaseTheme}"
                  TargetType="Separator">
        <Setter Property="Height" Value="1" />
    </ControlTheme>

    <ControlTheme x:Key="VerticalSeparatorTheme"
                  BasedOn="{StaticResource SeparatorBaseTheme}"
                  TargetType="Separator">
        <Setter Property="Width" Value="1" />
    </ControlTheme>

</ResourceDictionary>