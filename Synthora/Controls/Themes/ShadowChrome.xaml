<ResourceDictionary xmlns="https://github.com/avaloniaui"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    x:ClassModifier="internal"
                    x:CompileBindings="True">

    <ControlTheme x:Key="{x:Type ShadowChrome}" TargetType="ShadowChrome">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderBrush" Value="{DynamicResource ThemeBorderLowBrush}" />
        <Setter Property="BorderThickness" Value="{DynamicResource ThemeBorderThickness}" />
        <Setter Property="CornerRadius" Value="{DynamicResource ThemeBorderCornerRadiusLarge}" />
        <Setter Property="BoxShadow" Value="{DynamicResource DefaultBoxShadow}" />
        <Setter Property="InteractionBoxShadow" Value="{DynamicResource DefaultBoxShadow}" />
        <Setter Property="ClipToBounds" Value="False" />
        <Setter Property="Template">
            <ControlTemplate>
                <Border Name="RootBorder"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        BoxShadow="{TemplateBinding BoxShadow}"
                        CornerRadius="{TemplateBinding CornerRadius}">
                    <Border ClipToBounds="True">
                        <Border.CornerRadius>
                            <MultiBinding Converter="{x:Static SynthoraConverters.BorderCornerRadiusConverter}" ConverterParameter="{x:Static CornerRadiusType.All}">
                                <TemplateBinding Property="CornerRadius" />
                                <TemplateBinding Property="BorderThickness" />
                            </MultiBinding>
                        </Border.CornerRadius>
                        <ContentPresenter Name="PART_ContentPresenter"
                                          Margin="{TemplateBinding Padding}"
                                          HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                          Content="{TemplateBinding Content}"
                                          ContentTemplate="{TemplateBinding ContentTemplate}" />
                    </Border>
                </Border>
            </ControlTemplate>
        </Setter>
        <Style Selector="^[ShowShadowOnInteraction=True]">
            <Setter Property="BoxShadow" Value="{DynamicResource EmptyBoxShadow}" />
            <Style Selector="^:pointerover,^:focus-within">
                <Style Selector="^ /template/ Border#RootBorder">
                    <Setter Property="BoxShadow" Value="{TemplateBinding InteractionBoxShadow}" />
                </Style>
            </Style>
        </Style>
    </ControlTheme>

</ResourceDictionary>