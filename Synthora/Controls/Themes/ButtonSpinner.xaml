<ResourceDictionary xmlns="https://github.com/avaloniaui"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    x:ClassModifier="internal"
                    x:CompileBindings="True">

    <ControlTheme x:Key="SimpleButtonSpinnerRepeatButtonTheme"
                  BasedOn="{StaticResource {x:Type RepeatButton}}"
                  TargetType="RepeatButton">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderBrush" Value="Transparent" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Focusable" Value="False" />
        <Style Selector="^:pointerover">
            <Setter Property="Background" Value="{DynamicResource ThemeSelectedLowBrush}" />
        </Style>
        <Style Selector="^:pressed">
            <Setter Property="Background" Value="{DynamicResource ThemeSelectedMidBrush}" />
        </Style>
    </ControlTheme>

    <ControlTheme x:Key="{x:Type ButtonSpinner}" TargetType="ButtonSpinner">
        <Setter Property="Background" Value="{DynamicResource ThemeBackgroundBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource ThemeBorderMidBrush}" />
        <Setter Property="BorderThickness" Value="{DynamicResource ThemeBorderThickness}" />
        <Setter Property="CornerRadius" Value="{DynamicResource ThemeBorderCornerRadiusNormal}" />
        <Setter Property="Padding" Value="{DynamicResource ThemeControlPadding}" />
        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="Focusable" Value="True" />
        <Setter Property="Template">
            <ControlTemplate>
                <DockPanel>
                    <ContentPresenter HorizontalAlignment="{TemplateBinding (TextBoxAttach.HeaderHorizontalAlignment)}"
                                      VerticalAlignment="{TemplateBinding (TextBoxAttach.HeaderVerticalAlignment)}"
                                      Content="{TemplateBinding (TextBoxAttach.Header)}"
                                      DockPanel.Dock="{TemplateBinding (TextBoxAttach.HeaderPlacement)}" />
                    <DropShadowChrome Name="FocusTarget"
                                      Width="{TemplateBinding (TextBoxAttach.Width)}"
                                      MinWidth="{TemplateBinding (TextBoxAttach.MinWidth)}"
                                      MaxWidth="{TemplateBinding (TextBoxAttach.MaxWidth)}"
                                      Background="{TemplateBinding Background}"
                                      BorderBrush="{TemplateBinding BorderBrush}"
                                      BorderThickness="{TemplateBinding BorderThickness}"
                                      BoxShadow="{TemplateBinding (BoxShadowAttach.BoxShadow)}"
                                      CornerRadius="{TemplateBinding CornerRadius}"
                                      TemplatedControl.IsTemplateFocusTarget="True">
                        <DataValidationErrors>
                            <DockPanel>
                                <UniformGrid Name="PART_SpinnerPanel"
                                             DockPanel.Dock="Right"
                                             IsVisible="{TemplateBinding ShowButtonSpinner}"
                                             Rows="2">
                                    <RepeatButton Name="PART_IncreaseButton"
                                                  Padding="4,0"
                                                  IsTabStop="{TemplateBinding IsTabStop}"
                                                  Theme="{StaticResource SimpleButtonSpinnerRepeatButtonTheme}">
                                        <PathIcon Width="8"
                                                  Height="4"
                                                  HorizontalAlignment="Center"
                                                  VerticalAlignment="Center"
                                                  Data="{DynamicResource ArrowUpData}"
                                                  Foreground="{DynamicResource ThemeForegroundBrush}" />
                                    </RepeatButton>
                                    <RepeatButton Name="PART_DecreaseButton"
                                                  Padding="4,0"
                                                  IsTabStop="{TemplateBinding IsTabStop}"
                                                  Theme="{StaticResource SimpleButtonSpinnerRepeatButtonTheme}">
                                        <PathIcon Width="8"
                                                  Height="4"
                                                  HorizontalAlignment="Center"
                                                  VerticalAlignment="Center"
                                                  Data="{DynamicResource ArrowDownData}"
                                                  Foreground="{DynamicResource ThemeForegroundBrush}" />
                                    </RepeatButton>
                                </UniformGrid>
                                <ContentPresenter Name="PART_ContentPresenter"
                                                  Margin="{TemplateBinding Padding}"
                                                  HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                  VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                                  Content="{TemplateBinding Content}"
                                                  ContentTemplate="{TemplateBinding ContentTemplate}" />
                            </DockPanel>
                        </DataValidationErrors>
                    </DropShadowChrome>
                </DockPanel>
            </ControlTemplate>
        </Setter>

        <Style Selector="^ /template/ RepeatButton">
            <Setter Property="CornerRadius" Value="0" />
        </Style>
        <Style Selector="^:left">
            <Style Selector="^ /template/ UniformGrid#PART_SpinnerPanel">
                <Setter Property="DockPanel.Dock" Value="Left" />
            </Style>
        </Style>
        <Style Selector="^:pointerover">
            <Setter Property="BorderBrush" Value="{DynamicResource ThemeBorderHighlightBrush}" />
        </Style>
        <Style Selector="^:focus-within">
            <Setter Property="BorderBrush" Value="{DynamicResource ThemeBorderHighlightBrush}" />
        </Style>
        <Style Selector="^:error">
            <Setter Property="BorderBrush" Value="{DynamicResource ErrorBrush}" />
        </Style>
        <Style Selector="^:disabled /template/ DropShadowChrome#FocusTarget">
            <Setter Property="Opacity" Value="{DynamicResource ThemeDisabledLowOpacity}" />
        </Style>
    </ControlTheme>

</ResourceDictionary>