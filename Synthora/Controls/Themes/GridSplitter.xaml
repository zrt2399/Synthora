<ResourceDictionary xmlns="https://github.com/avaloniaui"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    x:ClassModifier="internal"
                    x:CompileBindings="True">

    <x:Int32 x:Key="GridSplitterPointerOverZIndex">1000</x:Int32>
    <x:Double x:Key="GridSplitterInnerBorderMaxLength">60</x:Double>

    <ControlTheme x:Key="{x:Type GridSplitter}" TargetType="GridSplitter">
        <Setter Property="Focusable" Value="True" />
        <Setter Property="MinWidth" Value="{DynamicResource GridSplitterMinLength}" />
        <Setter Property="MinHeight" Value="{DynamicResource GridSplitterMinLength}" />
        <Setter Property="ClipToBounds" Value="False" />
        <Setter Property="Background" Value="{DynamicResource GridSplitterOuterBorderBackground}" />
        <Setter Property="PreviewContent">
            <Template>
                <Rectangle Fill="{DynamicResource GridSplitterPreviewContentBackground}" />
            </Template>
        </Setter>
        <Setter Property="Template">
            <ControlTemplate>
                <Border Name="OuterBorder"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        CornerRadius="{TemplateBinding CornerRadius}">
                    <Border Name="InnerBorder"
                            MaxWidth="{DynamicResource GridSplitterInnerBorderMaxLength}"
                            MaxHeight="{DynamicResource GridSplitterInnerBorderMaxLength}"
                            Background="{DynamicResource GridSplitterInnerBorderBackground}"
                            CornerRadius="{DynamicResource ThemeBorderCornerRadiusNormal}" />
                </Border>
            </ControlTemplate>
        </Setter>

        <Style Selector="^:pointerover,^:pressed">
            <Setter Property="ZIndex" Value="{DynamicResource GridSplitterPointerOverZIndex}" />
            <Style Selector="^ /template/ Border#OuterBorder">
                <Setter Property="BoxShadow" Value="{TemplateBinding Background, Converter={x:Static SynthoraConverters.BoxShadowConverter}, ConverterParameter={x:Static BoxShadowConverter.ButtonPointerOverParameter}}" />
            </Style>
        </Style>
    </ControlTheme>
</ResourceDictionary>